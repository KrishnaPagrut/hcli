    import datetime module  (lines 1–1)
    class TextUtils
        method word_count(self, text)
            docstring explaining method returns number of words in a string  (lines 6–6)
            import re module, normalize text by removing punctuation and converting to lowercase, split into words and return count  (lines 8–10)
    class MathUtils
        method add(self, a, b)
            docstring explaining method returns sum of two numbers  (lines 15–15)
            return the sum of parameters a and b  (lines 16–16)
    when run as main: create instances of TextUtils and MathUtils, test word_count method with 'hello world' and add method with 2 and 3, printing both results  (lines 19–23)